[workspace]

members = [
    "sentence",
    "visual",
    "util",
    "pgem"
]

[package]
name = "simplembed"
version = "0.1.0"
edition = "2021"

[dependencies]
sentence = { path = "sentence" }
visual = { path = "visual" }
util = {path = "util"}

clap = "4.4.11"
